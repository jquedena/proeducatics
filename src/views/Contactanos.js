import React from 'react';
import Layout from './../components/Layout';

const Contactanos = props => {
    return (
        <Layout className="nosotros">
        <div>
            Contactanos
        </div>
        </Layout>
    );
};

export default Contactanos; 